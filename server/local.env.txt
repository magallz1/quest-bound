PORT=8000
MODE=local

# Use whatever DB name and password you'd like
DATABASE_NAME=qbdb
DATABASE_PASSWORD=password

# The DATABASE_NAME should match the string following the port number
# The DATABASE_PASSWORD should follow 'postgres:'
DATABASE_URL="postgres://postgres:password@localhost:6543/qbdb?pgbouncer=true"
DIRECT_URL="postgres://postgres:password@localhost:5432/qbdb"

